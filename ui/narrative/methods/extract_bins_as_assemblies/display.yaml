#
# define display information
#
name: Extract Bins as Assemblies from BinnedContigs - UI EXPERIMENTATION
tooltip: testing test testing
screenshots: []

icon: binutil-green.png

#
# define a set of similar methods that might be useful to the user
#
suggestions:
    apps:
        related:
            []
        next:
            []
    methods:
        related:
            []
        next:
            []

#
# Configure the display and description of parameters
#
parameters :
    binned_contig_obj_ref :
        ui-name : <b>Binned</b> Contigs
        short-hint : the <b>binned contig</b> data to edit
    add_all_bins :
        ui-name : Add all bins
        short-hint : extract all bins in the binned contig object
    extracted_assemblies:
        ui-name : Bin Names For Extraction
        short-hint : Select the bins you want extracted as assemblies, one bin per assembly.
    assembly_suffix:
        ui-name : Assembly Name Suffix
        short-hint : provide a suffix append to Bin name for new Assembly name
    assembly_set_name:
        ui-name : Assembly Set Name
        short-hint : multiple assemblies will be collected in a set with this name (required if more than one assembly is selected)
        long-hint: |
                 Vot is dis!
            

description : |
    <p>Basic tool for extracting bins from BinnedContig data.</p>

